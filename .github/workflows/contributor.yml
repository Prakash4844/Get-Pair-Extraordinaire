name: contributor

on: workflow_dispatch
#  workflow_run:
#    workflows: ["Pair_Extraordinaire"]
#    types:
#      - completed

jobs:
  contrib-readme-job:
    runs-on: ubuntu-latest
    name: A job to automate contrib in readme
    steps:
      - name: Contribute List
        uses: akhilmhdh/contributors-readme-action@v2.3.6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          image_size: 100 # Size of square images in the stack
          readme_path: Extraordinary.md # Path to the readme file